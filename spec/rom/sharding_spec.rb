require 'spec_helper'

describe Rom::Sharding do
  it 'has a version number' do
    expect(Rom::Sharding::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
